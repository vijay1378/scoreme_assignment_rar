# Task 5 — Implementation

Two implementations are included:

- `python/` — permitted Python 3.10+ implementation from the assignment workflow.
- `java21/` — Java 21 version prepared for environments where Java approval is available.

The assignment text says Python 3.10+ is accepted and Java 17+ requires prior written approval, so confirm approval before submitting the Java version.
